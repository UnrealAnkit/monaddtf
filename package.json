{
  "name": "alloy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network monadTestnet",
    "dev": "npm --prefix app run dev"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "A single-deposit, immutable-weight index vault for the Monad ecosystem.",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^26.4.0",
    "dotenv": "^17.4.2",
    "hardhat": "^2.29.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
